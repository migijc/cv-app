import React from "react";

export default class Header extends React.Component{
    render(){
        return (
            <div id="header">
                <h1 className="pageTitle">OnlyResumes</h1>
            </div>
        )
    }
}